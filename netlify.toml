[build]
  command = "pip install -r requirements.txt"
  functions = "functions"
  publish = "public"

# [functions] block is REMOVED intentionally for this test.
# Let's see if it finds the files without extra config.

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app_handler"
  status = 200